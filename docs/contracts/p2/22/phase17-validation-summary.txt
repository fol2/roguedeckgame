Phase 17 hardening/refactor validation summary
Date: 2026-05-28
Working tree: /tmp/roguedeckgame-d3
Input ZIP: /mnt/data/roguedeckgame-review-d3fa10bbb027.zip

Scope of changes:
- Hardened default Vitest gate so `npm test` uses a bounded threads pool, explicit config, max workers, and 20s per-test timeout.
- Added explicit test lanes: test:cli, test:core, test:phaser, test:scripts.
- Hardened CLI spawn tests with explicit process timeout and signal assertions.
- Hardened localhost server cleanup with idle/all connection close calls.
- Hardened Vite preview integration browser smoke for root Linux/Chromium by adding no-sandbox args, DevTools polling, process cleanup, and optional fallback when local browser policy blocks localhost/loopback navigation.
- Tightened production-facing RunSandboxController mutation APIs so map select, reward claim/skip, and non-combat completion require expected revision + request id.
- Updated preview/test call sites to pass explicit revision + request id.

Validation results:
- npm ci: PASS, 0 vulnerabilities.
- npm run typecheck: PASS.
- npm test -- --reporter=dot: PASS, 110 test files, 842 tests.
- npm run build: PASS.
- npm run build:cli: PASS.
- npm run test:integration: PASS, 2 files, 4 tests.
  Note: in this container, Chrome policy blocks localhost/loopback navigation, so the optional browser DOM smoke logs a skip warning after HTTP preview/assets are validated. Set REQUIRE_BROWSER_LOCALHOST_SMOKE=1 to require browser DOM smoke as a hard failure.
- npm run sim:smoke -- --analyze: PASS, 3/3 completed, 0 failures, 193 content dependency references, 0 missing, 0 warnings, health no issues.
- npm run sim:balance: PASS, 200 fuzz runs, completion 51.5%, target 45.0%-60.0%, 0 failures, health no issues.
- npm audit --audit-level=moderate: PASS, 0 vulnerabilities.

Caveats / next hardening still recommended:
- This pass fixed the reliability gate and tightened mutation API contracts, but it did not split the largest modules.
- Remaining refactor targets: RunSandboxController, combat-scene-orchestrator, combat-view-model, validation.ts, save.ts, and content-workbench modules.
- Integration browser DOM smoke is optional by default to keep clean container validation reliable; CI/dev machines that support Chrome localhost navigation can enforce it with REQUIRE_BROWSER_LOCALHOST_SMOKE=1.
